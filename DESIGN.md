# DESIGN.md

## 1. 项目定位

这是一个纯前端的背单词卡片应用，目标是让用户在短时间内完成高频复习，并保留轻量、快速、离线可用的体验。页面的核心是单词卡片翻转、复习反馈、图片卡片支持以及本地存储。设计重点是：清晰、专注、可快速扫描，避免视觉噪音干扰记忆。

## 2. 配色方案

### 2.1 整体色板

- 背景色：`#edf5ff` 到 `#f6f0ff` 的渐变
  - 作用：建立轻盈、清爽、学习场景的基础氛围。
- 卡片底色：`rgba(255, 255, 255, 0.97)`
  - 作用：卡片作为主要信息容器，视觉上轻且干净。
- 文字主色：`#172033`
  - 作用：用于单词、标题、释义等高重要度内容。
- 次要文字：`#5f6f8a`
  - 作用：用于状态说明、提示、备注等辅助信息。
- 主按钮色：`#4f7cff` → `#355eea`
  - 作用：用于默认操作、确认、强调行为。
- 次按钮色：`#ffffff` + 蓝色边框
  - 作用：给出非破坏性操作，保持低调但清晰。
- 记得按钮：`#2ecb8d` → `#19a76f`
  - 作用：正向反馈，视觉上表现为“正确、已记住”。
- 忘了按钮：`#ff7a59` → `#ef5d42`
  - 作用：否定反馈，适当突出但不刺眼。

### 2.2 色彩原则

- 整体保持低饱和、低噪音的蓝紫渐变背景。
- 主要信息使用高对比度的深色文字，确保阅读清晰。
- 按钮和状态颜色不超过两个明显强度层级，避免视觉疲劳。
- 强调色只出现在关键动作与状态反馈区域。

## 3. 字体层级

### 3.1 字体家族

- 使用系统字体栈：`-apple-system`, `BlinkMacSystemFont`, `Segoe UI`, `PingFang SC`, `Microsoft YaHei`, sans-serif。
- 目标：中文显示稳定，天然适配桌面和移动端。

### 3.2 层级规则

- 标题：`clamp(1.55rem, 2vw, 2.15rem)`，字重 `800`
  - 用于页面主标题和顶栏主文案。
- 单词正面：`clamp(2.5rem, 8vw, 4.1rem)`，字重 `800`，字母间距 `0.08em`
  - 这是用户最核心的记忆对象，字号需要大且精准。
- 背面单词：`clamp(2.2rem, 7vw, 3.2rem)`，字重 `800`
  - 仍然强调单词，但比正面略收敛。
- 释义/中文：`clamp(1.2rem, 3vw, 1.7rem)`，字重 `700`
  - 用于表达意思，长度较长时适度收紧。
- 备注/辅助文字：`0.9rem ~ 1rem`，字重 `500 ~ 600`
  - 只作补充说明，不强调主信息。
- 按钮文字：`1.08rem`，字重 `800`
  - 对关键动作有足够清晰度。

## 4. 间距规则

- 页外容器与页面边缘：`16px ~ 24px`。
- 顶栏与卡片之间：`18px ~ 20px`。
- 卡片内边距：`22px ~ 28px`。
- 卡片内部元素间距：`10px ~ 14px`。
- 按钮组之间：`14px`。
- 模块之间的留白：保持统一的 12px 到 18px 规律，避免节奏散乱。
- 弹窗内部：上下留白统一为 18px，左右 20px。

## 5. 圆角与阴影规范

### 5.1 圆角

- 卡片：`30px`
- 按钮：`14px ~ 18px`
- 输入框和小组件：`10px ~ 12px`
- 图片容器：`18px ~ 20px`

### 5.2 阴影

- 卡片：`0 24px 54px rgba(33, 64, 142, 0.12)`
- 按钮：`0 12px 24px rgba(30, 41, 59, 0.12)`
- 轻微悬停：`0 12px 22px rgba(79, 124, 255, 0.12)`

目标：影子不重、不浮夸，突出层级感但不破坏简洁度。

## 6. 动效规范

### 6.1 翻卡

- 过渡时长：`0.7s`
- 缓动曲线：`cubic-bezier(0.2, 0.8, 0.2, 1)`
- 作用：让翻转有“轻快但稳定”的感觉，突出复习流程的节奏。

### 6.2 按钮交互

- 悬停：`translateY(-1px)` + 轻微阴影增强
- 点击：`scale(0.98)` 或 `translateY(0)`
- 过渡时长：`0.22s`
- 作用：反馈及时、清晰，并保持学习场景的舒适感。

### 6.3 图片与卡片微交互

- 图片区域轻微上浮，说明其为可视主体。
- 但不做夸张动效，避免影响单词学习与注意力集中。

## 7. 设计目标

这款应用的设计目标不是“炫”，而是“稳定、清晰、可持续复习”。因此设计语言应遵循以下原则：

- 最重要的信息永远最大、最清楚。
- 颜色和层级帮助用户识别状态，不制造额外认知负担。
- 保持学习场景的舒适，避免过度鲜艳或过于复杂的装饰。
- 所有交互应尽量自然，像真实的纸质学习卡片一样轻盈、有序。

## 8. 使用建议

在后续迭代中，建议保持以下原则：

1. 不在卡片中堆叠过多颜色。
2. 不让非核心信息抢夺单词视觉焦点。
3. 主要操作始终高于次要操作。
4. 优先保证可读性和流畅交互，再考虑“高级感”的完成度。
